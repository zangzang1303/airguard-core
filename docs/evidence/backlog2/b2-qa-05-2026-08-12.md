# B2-QA-05 — Release checklist evidence

## Kết luận

**Trạng thái:** TECHNICAL PASS — 5/5 kiểm tra kỹ thuật đã hoàn tất.

**Release sign-off:** PENDING — còn Leader review và Mentor xác nhận các quyết định provisional.

Không tuyên bố release được phê duyệt cho tới khi hai xác nhận bên ngoài được ghi tên, thời gian và quyết định vào tài liệu này.

## Run metadata

| Trường | Giá trị |
|---|---|
| Thời gian kiểm tra | `2026-08-12T01:39:48+07:00` đến `2026-08-12T01:42:29+07:00` |
| Branch | `hvlong` |
| Commit baseline | `e305f34` |
| Docker | client/server `29.6.2` |
| Docker Compose | `v5.3.1` |
| Runtime | 8 service Compose; backend, Agent và PostgreSQL có health check |

Worktree có thay đổi chưa commit. Các kiểm tra dưới đây được chạy trên đúng worktree đó.

## 1. `.env` không nằm trong Git diff

### Commands

```powershell
git ls-files --error-unmatch -- .env
git status --short -- .env
git diff --name-only -- .env
git check-ignore -v .env
```

### Actual

```text
env_tracked=False
env_status_count=0
env_diff_count=0
.gitignore:16:.env    .env
```

**Result:** PASS — `.env` được ignore, không tracked và không nằm trong diff.

## 2. Không có secret/PII trong release evidence

Đã quét các file text trong `docs/evidence/backlog2` bằng mẫu nhận diện access key, token, private key, bearer credential, secret/password assignment, email và đường dẫn Windows user profile.

### Actual

```text
release_text_files=4
secret_like_hits=0
pii_like_hits=0
release_images=2
```

Hai screenshot QA-04 cũng đã được kiểm tra trực quan: ảnh dashboard được crop khỏi khu vực profile/credential; ảnh còn lại chỉ chứa simulator disclaimer.

Transcript thô `.tmp/b2-qa-02/run-20260811-233742.txt` có một đường dẫn Windows user profile. File này **không thuộc release evidence và không được commit/publish**. File được giữ nguyên để không tự ý xóa evidence cục bộ của operator.

**Result:** PASS cho bộ release evidence; có guardrail rõ cho file tạm cục bộ.

## 3. Compose config

### Command

```powershell
docker compose config --quiet
```

### Actual

```text
exit_code=0
services=mqtt,postgres,backend,mqtt-consumer,sensor-simulator,agent,device-simulator,frontend
```

**Result:** PASS.

## 4. Runbook khớp command thật

Runbook được kiểm tra: `docs/team-setup-and-demo.md`.

### Commands replayed

```powershell
docker version
docker compose version
docker compose config --quiet
docker compose up -d postgres mqtt
.\scripts\init-demo-db.ps1
docker compose up -d --build `
  backend agent frontend mqtt-consumer `
  sensor-simulator device-simulator
docker compose ps
Invoke-RestMethod http://localhost:8000/health
Invoke-RestMethod http://localhost:8000/ready
Invoke-RestMethod http://localhost:8001/health
Invoke-RestMethod http://localhost:8000/api/v1/stations
Invoke-WebRequest http://localhost:5173 -UseBasicParsing
```

### Actual

```text
init-demo-db=PASS; idempotent replay retained S01,S02,S03,S04,S05
compose_build_start_exit_code=0
services_running=8/8
backend_health=ok
backend_ready=ready
agent_health=ok
station_count=5
station_ids=S01,S02,S03,S04,S05
frontend_status=200
```

Ngay sau recreate, `docker compose ps` có thể tạm hiển thị Agent là `health: starting`; endpoint `/health` đã trả `ok`. Health cuối được kiểm tra lại trước khi chốt evidence.

**Result:** PASS — service names, script, ports và endpoint trong runbook khớp runtime hiện tại.

## 5. Task status

`tasks/backlog2/qa-integration-evidence.md` đã được cập nhật:

- B2-QA-01: hoàn tất;
- B2-QA-02: hoàn tất;
- B2-QA-03: hoàn tất;
- B2-QA-04: hoàn tất;
- B2-QA-05: 5 mục kỹ thuật hoàn tất; 2 mục sign-off bên ngoài đang chờ.

**Result:** PASS.

## 6. External release sign-off

Hai mục này không thể do QA/Codex tự xác nhận:

| Gate | Trạng thái | Evidence cần bổ sung |
|---|---|---|
| Leader review toàn bộ evidence QA-01 đến QA-05 | `PENDING` | Tên reviewer, thời gian, commit/worktree được review, `APPROVED` hoặc danh sách finding |
| Mentor xác nhận quyết định provisional | `PENDING` | Tên Mentor, thời gian, quyết định được xác nhận hoặc đường dẫn biên bản |

Các quyết định provisional cần Mentor rà soát theo `planning/dependencies.md`:

1. threshold, severity và cooldown của alert;
2. tên/toạ độ chính thức S01–S05;
3. Weather API, key ownership và fallback;
4. manager role/demo account và hướng RBAC;
5. LLM provider/key và chính sách fallback;
6. phạm vi device action/dispatcher.

Mẫu ghi xác nhận:

```text
Leader review: [APPROVED / CHANGES REQUESTED]
Reviewer: <tên>
Reviewed at: <ISO-8601 timestamp>
Reviewed revision: <commit hoặc mô tả worktree>
Findings/evidence link: <đường dẫn>

Mentor provisional decisions: [CONFIRMED / CHANGES REQUIRED]
Mentor: <tên>
Confirmed at: <ISO-8601 timestamp>
Decision record/link: <đường dẫn>
```

## Known limitations

- QA-05 chưa phải release approval cuối cùng khi hai sign-off trên còn `PENDING`.
- `.tmp/` là dữ liệu chạy cục bộ, không phải release evidence; transcript QA-02 thô chứa đường dẫn user profile nên không được commit/publish.
- Tài khoản/mật khẩu demo trong runbook chỉ dành cho local demo; không đưa chúng vào screenshot hoặc log phát hành.

## Evidence index

- [B2-QA-01](b2-qa-01-2026-08-11.md)
- [B2-QA-02](b2-qa-02-2026-08-11.md)
- [B2-QA-03](b2-qa-03-2026-08-12.md)
- [B2-QA-04](b2-qa-04-2026-08-12.md)
- B2-QA-05: tài liệu hiện tại
