# B2-QA-01 — Environment smoke evidence

## Kết luận

**Trạng thái:** PASS — 4/4 tiêu chí đã được QA xác nhận.

- PASS: clean Compose startup bằng PostgreSQL volume mới.
- PASS: restart bằng PostgreSQL volume hiện có, không mất dữ liệu và seed idempotent.
- PASS: frontend ở port 5173 và fallback Vite ở port 5174.
- PASS: Docker Desktop stopped -> lỗi kết nối rõ ràng -> restart và recovery thành công.
- PASS: operator xác nhận đây là lần đầu máy chạy P-074; timestamp tạo volume mặc định và clean-volume replay corroborate xác nhận này.

Không dùng fixture để thay cho runtime. Không ghi secret, nội dung `.env`, username hoặc đường dẫn home vào evidence này.

## Run metadata

| Trường | Giá trị |
|---|---|
| Bắt đầu | `2026-08-11T22:58:14.8218265+07:00` |
| Kết thúc | `2026-08-11T23:17:43.1823203+07:00` |
| Branch | `hvlong` |
| Commit | `e305f34b6e6ed1e379356e9fa2543fcc82b09b6f` |
| Docker Engine | `29.6.2` |
| Docker Desktop | `4.85.0` |
| Docker Compose | `v5.3.1` |
| Compose project mặc định | `p-074` |
| Compose project clean QA | `p074-qa-clean-20260811` |

Worktree không sạch trước khi chạy QA. Các file đã modified được giữ nguyên, không reset:

```text
scripts/log_hook.py
src/agents/graph.py
src/agents/nodes/orchestration.py
src/agents/response_composer.py
tasks/backlog2/qa-integration-evidence.md
tests/test_agents/test_grounding.py
```

## Scenario 1 — Baseline và Compose validation

### Commands

```powershell
Get-Date -Format o
git branch --show-current
git rev-parse HEAD
git status --short
docker version
docker compose version
docker compose config --quiet
docker compose ps --all
```

### Expected

- Docker client kết nối được Docker Engine.
- Compose file hợp lệ.
- Baseline branch/commit/worktree được ghi trước khi test.

### Actual

- Docker Engine và Compose hoạt động.
- `docker compose config --quiet` exit code `0`.
- Baseline stack có đủ 8 service; PostgreSQL, backend và Agent healthy.

**Result:** PASS.

## Scenario 2 — Existing PostgreSQL volume

Volume được xác định trước khi restart: `p-074_postgres_data`.

### Commands

```powershell
docker compose exec -T postgres psql -U airguard -d airguard -At `
  -c "SELECT count(*) FROM measurements;"
docker compose down
docker volume inspect p-074_postgres_data --format '{{.Name}}'
docker compose up -d postgres mqtt
.\scripts\init-demo-db.ps1
docker compose exec -T postgres psql -U airguard -d airguard -At `
  -c "SELECT 'stations=' || count(*) FROM stations; SELECT 'measurements=' || count(*) FROM measurements; SELECT 'station_ids=' || string_agg(station_id, ',' ORDER BY station_id) FROM stations;"
```

### Expected

- `docker compose down` không xóa named volume vì không dùng `-v`.
- Seed chạy lại được và không nhân đôi station.
- Measurement hiện có không bị mất.

### Actual

```text
measurements_before_restart=195
volume_after_down=p-074_postgres_data
stations_after_restart=5
measurements_after_restart=195
station_ids_after_restart=S01,S02,S03,S04,S05
```

Schema trả các `NOTICE ... already exists, skipping`, đúng với hành vi idempotent; command exit code `0`.

**Result:** PASS.

## Scenario 3 — Clean startup bằng volume QA mới

Để không xóa hoặc ghi đè volume thật, clean startup dùng Compose project riêng.

### Commands

```powershell
docker compose down
docker compose -p p074-qa-clean-20260811 up -d --build `
  postgres mqtt backend agent frontend mqtt-consumer sensor-simulator device-simulator
$env:COMPOSE_PROJECT_NAME='p074-qa-clean-20260811'
docker compose up -d --wait `
  postgres mqtt backend agent frontend mqtt-consumer sensor-simulator device-simulator
.\scripts\init-demo-db.ps1
docker compose ps
```

### Expected

- Compose tạo volume PostgreSQL mới.
- Tất cả service start được.
- PostgreSQL/backend/Agent qua health gate.
- S01–S05 tồn tại và simulator tạo measurement thật qua runtime.

### Actual

```text
new_volume=p074-qa-clean-20260811_postgres_data
services_running=8/8
postgres=healthy
backend=healthy
agent=healthy
stations=5
measurements=15
station_ids=S01,S02,S03,S04,S05
```

API smoke:

```text
GET /health                 -> status=ok
GET /ready                  -> status=ready, database=ok
GET agent:8001/health       -> status=ok
GET /api/v1/stations        -> 5 items, S01-S05
GET frontend:5173           -> HTTP 200, root element present
```

**Result:** PASS trên clean Compose volume; kết quả này replay thành công bootstrap runtime của lần setup đầu.

## Scenario 4 — Port 5173/5174

### Commands

```powershell
Invoke-WebRequest http://localhost:5173 -UseBasicParsing
npm.cmd --prefix frontend run dev -- --host 127.0.0.1 --port 5174
Invoke-WebRequest http://127.0.0.1:5174 -UseBasicParsing
```

Tiến trình Vite 5174 được chạy ẩn trong thời gian test, xác minh theo PID/process name rồi dừng; không dừng frontend Docker ở 5173.

### Actual

```text
frontend_5173_status=200
frontend_5173_has_root=true
frontend_5174_status=200
frontend_5174_has_root=true
port_5174_after_cleanup=free
```

**Result:** PASS.

## Scenario 5 — Docker Desktop not running và recovery

### Commands

```powershell
docker desktop stop
docker desktop status
docker version
docker desktop start
docker compose -p p074-qa-clean-20260811 up -d --wait `
  postgres mqtt backend agent frontend mqtt-consumer sensor-simulator device-simulator
docker compose -p p074-qa-clean-20260811 ps
```

### Expected

- Khi Docker Desktop tắt, CLI phải báo lỗi rõ thay vì giả vờ startup thành công.
- Sau khi bật lại, stack phải recover và qua health gate.

### Actual

```text
docker_desktop_status_exit=1
docker_version_exit=1
Could not retrieve status. Is Docker Desktop running?
failed to connect to the docker API ... The system cannot find the file specified.

recovery_status=running
recovery_services=8/8
postgres/backend/agent=healthy
```

**Result:** PASS.

## Environment restoration

Sau test, clean QA stack được `down` không có `-v`; stack mặc định `p-074` được khởi động lại với volume gốc.

```text
default_services_running=8/8
default_postgres=healthy
default_stations=5
default_measurements=210
default_station_ids=S01,S02,S03,S04,S05
preserved_volume=p-074_postgres_data
preserved_qa_volume=p074-qa-clean-20260811_postgres_data
```

Không volume nào bị xóa.

## Xác nhận lần setup đầu trên máy

**Expected:** máy chưa từng chạy runtime P-074 trước lượt setup, sau đó thực hiện `docs/team-setup-and-demo.md` và đạt các health/data checks.

**Operator confirmation:** đây là lần đầu P-074 được setup và chạy trên máy kiểm thử này.

**Dấu vết hỗ trợ:**

```text
git clone reflog=2026-07-26T13:34:23+07:00
default_postgres_volume=p-074_postgres_data
default_volume_created=2026-08-11T15:52:05Z
default_volume_created_local=2026-08-11T22:52:05+07:00
qa_baseline_started=2026-08-11T22:58:14+07:00
```

Volume runtime mặc định được tạo khoảng 6 phút trước baseline QA, phù hợp với xác nhận đây là lần khởi tạo runtime đầu. Sau đó clean Compose project/volume độc lập đã replay thành công toàn bộ startup, health và S01-S05.

Không có raw transcript của vài phút đầu trước baseline; vì vậy evidence được mô tả chính xác là **operator confirmation + system timestamp corroboration + clean replay**, không phải bản ghi terminal nguyên vẹn của toàn bộ lần chạy đầu.

## Checklist B2-QA-01

- [x] Test máy mới theo `docs/team-setup-and-demo.md` — lần đầu chạy trên máy, có operator confirmation, timestamp corroboration và clean replay.
- [x] Test clean startup và existing PostgreSQL volume.
- [x] Test port 5173/5174 và Docker Desktop not running.
- [x] Ghi blocker thật, không che bằng fixture.
