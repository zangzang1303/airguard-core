# B2-QA-02 — Automated tests evidence

## Kết luận

**Trạng thái:** PASS — 6/6 tiêu chí.

## Run metadata

| Trường | Giá trị |
|---|---|
| Bắt đầu | `2026-08-11T23:30:33.9123353+07:00` |
| Kết thúc | `2026-08-11T23:31:11.4446937+07:00` |
| Branch | `hvlong` |
| Commit baseline | `e305f34b6e6ed1e379356e9fa2543fcc82b09b6f` |
| Python | `.venv` project interpreter |
| LangSmith trong test | tracing tắt để không gửi telemetry ngoài test scope |

Worktree có thay đổi chưa commit và đã được ghi trong evidence B2-QA-01; không reset hoặc che các thay đổi đó.

## Commands và kết quả

### Compile backend/src/services

```powershell
.\.venv\Scripts\python.exe -m compileall -q backend src services
```

```text
exit_code=0
```

### Backend service/API tests

```powershell
$env:LANGCHAIN_TRACING_V2='false'
$env:LANGSMITH_TRACING='false'
.\.venv\Scripts\python.exe -m pytest -q tests/test_backend
```

```text
16 passed, 1 warning in 0.17s
exit_code=0
```

### MQTT validator/simulator/device tests

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests/test_iot
```

```text
13 passed in 0.14s
exit_code=0
```

### Frontend production build

```powershell
npm.cmd --prefix frontend run build
```

```text
TypeScript compile passed
Vite transformed 2279 modules
dist/index.html generated
build completed in 12.36s
exit_code=0
```

### Full pytest

```powershell
$env:LANGCHAIN_TRACING_V2='false'
$env:LANGSMITH_TRACING='false'
.\.venv\Scripts\python.exe -m pytest -q
```

```text
127 passed, 1 warning in 1.09s
exit_code=0
```

### Diff whitespace check

```powershell
git diff --check
```

```text
exit_code=0
```

## Known limitations/warnings

- Starlette báo deprecation warning cho `fastapi.testclient`/`httpx`; không làm test fail.
- Vite báo JavaScript chunk khoảng `921.44 kB`, lớn hơn ngưỡng khuyến nghị 500 kB; build vẫn pass nhưng nên code-split sau MVP.
- Git cảnh báo LF sẽ được chuyển thành CRLF khi Git chạm các file trên Windows; `git diff --check` vẫn exit `0`.
- LangSmith cloud tracing không thuộc test assertion và API key local đang trả 403, nên tracing được tắt trong pytest để tránh telemetry làm nhiễu output.

## Checklist

- [x] Compile backend/src/services.
- [x] Chạy backend service/API tests.
- [x] Chạy MQTT validator/simulator/device tests.
- [x] Chạy frontend build.
- [x] Cài đủ test dependency và chạy full pytest.
- [x] Chạy `git diff --check`.

