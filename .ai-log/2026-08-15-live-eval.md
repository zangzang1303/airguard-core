# AI Work Log

## Date / agent / machine

2026-08-15 / Codex / local workspace

## Goal

Implement Gate 2 live LLM evaluation evidence for five manual, replayable cases.

## Context read

`AGENTS.md`, `tasks/backlog3/qa-live-evidence.md`, `tasks/backlog3/agent-live-llm.md`,
`docs/agent-evaluation.md`, API contract, Agent graph and grounding tests.

## Files changed

- `eval/run_live_evaluation.py`
- `src/agents/nodes/orchestration.py`
- `tests/test_scripts/test_live_evaluation.py`
- `tests/test_agents/test_grounding.py`
- `docs/agent-evaluation.md`
- `tasks/backlog3/qa-live-evidence.md`
- `docs/evidence/.gitkeep`

## Decisions and rationale

- The runner calls the canonical backend endpoint, not the isolated Agent service.
- A case passes only with `generation_mode=live_llm`; fallback, missing metadata, tool mismatch,
  wrong outcome, or request ID mismatch is BLOCKED.
- The model may append only a non-factual explanation after deterministic composition for insufficient
  data and safety refusal outcomes. The policy still owns all decisions, facts and HITL boundaries.

## Commands/tests run and results

- `pytest tests/test_scripts/test_live_evaluation.py tests/test_agents/test_grounding.py -q`: 41 passed.
- `ruff check --fix ...`: clean after import formatting.
- `git diff --check`: passed.
- Runtime check: local OpenAI key is configured, but Docker daemon/API services are unavailable;
  therefore no live evidence was generated and no fabricated PASS was recorded.

## Contracts/risks changed

No REST or tool-schema change. Live provider calls can incur normal configured-provider usage only
when `OPENAI_API_KEY` is present; generated evidence is sanitized for secret-like field names.

## Blockers/open questions

Docker daemon is not running/accessible, so backend `:8000` and Agent `:8001` were unavailable.

## Next exact step

Start Docker Desktop, run `docker compose up -d --build`, wait for fresh simulator history, then run
`.\.venv\Scripts\python.exe eval\run_live_evaluation.py` and inspect the generated pack under
`docs/evidence/release/`.

## Handoff IDs (request/message/proposal/job)

None; live requests were not sent because the runtime was unavailable.
