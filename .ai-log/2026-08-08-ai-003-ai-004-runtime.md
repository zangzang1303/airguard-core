# AI Work Log

## Date / agent / machine

2026-08-08 / Codex / Windows workspace

## Goal

Complete the Person 1 workstream: AI-003 personalized recommendations, AI-004 forecast-aware
responses, graph/request integration and a real backend-to-Agent runtime path.

## Context read

`AGENTS.md`, `README.md`, `tasks/ai-agent.md`, `planning/ai-agent-parallel-work-plan.md`, ADR 0004,
ADR 0006, API contracts, security/DoD docs, current Agent graph/tools/tests, backend runtime,
Compose and frontend Agent client.

## Files changed

- Agent policy/runtime: recommendation and forecast policies, route/state/orchestration/composer,
  API schemas/routes.
- Runtime: backend Agent proxy, settings, Celery Agent task, root Dockerfile and Compose Agent
  service.
- Frontend: pass stable user/station context and remove fabricated Agent fallback facts.
- Tests: recommendation, forecast, API context propagation and backend proxy correlation.
- Contracts/docs: API contract, ADR 0004/0006, environment setup and demo runbook.

## Decisions and rationale

- Keep recommendation policy deterministic and versioned outside prompts.
- Require backend profile lookup; never trust a client-supplied user group.
- Keep forecast distinct from observation and disclose absent metadata/low confidence.
- Run Agent as an isolated service with backend HTTP access only. Backend remains the canonical API
  and system of record.
- Propagate and verify request correlation id across backend and Agent.

## Commands/tests run and results

- `.venv\Scripts\python.exe -m pytest tests/test_agents tests/test_api/test_routes.py tests/test_backend/test_agent_service.py -q` -> 72 passed.
- `npm run build` in `frontend/` -> passed.
- `docker compose config --quiet` -> passed.
- Scoped Ruff checks for all changed Python files -> passed; `backend/app/main.py` was checked
  with the repository's pre-existing `UP017` datetime modernization rule ignored.
- Full `.venv\Scripts\python.exe -m pytest -q` -> collection blocked because the root virtualenv
  does not contain the backend dependency `psycopg2`.
- `docker compose build agent backend` -> blocked because Docker Desktop/Linux engine was not
  running; Compose configuration itself was valid.

## Contracts/risks changed

`POST /api/v1/agent/chat` now requires `message` and `user_id`, with optional `station_id`, at the
canonical backend boundary. Recommendation responses may include `recommendation_policy_version`.

Live recommendation still depends on the Person 2 contract-reconciliation work: profile
`user_group` aliasing, explicit weather freshness, alert source metadata and matching backend
fixtures/seed. The Person 1 graph fails closed until those tool outputs validate.

## Blockers/open questions

- Backend profile/alert/weather output drift is intentionally left to the Person 2 workstream.
- Product/Mentor still needs to confirm the final recommendation policy wording/bands.
- No full live Docker smoke with MQTT/PostgreSQL data was run in this session.
- Re-run the full suite after installing backend requirements in an appropriate environment and
  build/start the images after Docker Desktop is available.

## Next exact step

Merge the Person 2 tool/HITL/evaluation branch, rebase this workstream, then wire the provided
`run_proposal_workflow` into the graph and run full Compose smoke scenarios.

## Handoff IDs (request/message/proposal/job)

Fixture request ids only; no external proposal or device action was created.
