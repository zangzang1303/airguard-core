# AI Session Log — Phiên 3F (24/08/2026)

## 1. Mục tiêu phiên
- Đóng Phiên 3F — Xác minh khả năng phục hồi (Resilience) & Fault Injection của AI-24 trong môi trường trình duyệt thật (Playwright / Chromium).
- Thực hiện đầy đủ 3 kịch bản lỗi cô lập: HTTP 503, Client Timeout (> 10s), và Network Transport Failure (`socket.destroy()`).
- Đạt kết quả chính thức: **32 PASS, 0 FAIL, 0 BLOCKED**.

## 2. Bảo mật & Tiêu chuẩn Thu thập Bằng chứng
- Đã triển khai bộ lọc Allowlist Headers (`content-type`, `origin`, `accept`, `x-request-id`, `x-fault-mode`, `x-scenario-id`, `user-agent`) trong recorder proxy E2E.
- Đã loại bỏ hoàn toàn các header nhạy cảm (`Cookie`, `Authorization`, `X-CSRF-Token`) khỏi toàn bộ file bằng chứng `docs/evidence/session-3f/browser_e2e_evidence.json`.
- Khuyến nghị người dùng làm mới phiên đăng nhập local nếu có.

## 3. Khóa Phụ thuộc & Quản lý Runtime
- Đã thêm `playwright-core` vào `frontend/package.json` và đồng bộ hóa với `frontend/package-lock.json`.
- Đã xác thực khả năng tái lập bằng `npm ci` thành công (117 packages audited, 0 errors).
- Môi trường chạy Compose là development runtime với bind-mounts `./frontend/src:/app/src`.
- Runtime Compose vẫn là development topology với bind-mount frontend source; không dùng bind-mount cho Gate 5/6. Host `.venv\Scripts\python.exe --version` lỗi WindowsApps missing nên host pytest/evaluator không được công nhận.

## 4. Bằng chứng Thực thi Mạng (7 Proxy Records)
- **Ordinal 1 (503)**: Trả HTTP 503 `agent_unavailable`, UI hiển thị thông báo gián đoạn `role="alert"`.
- **Ordinal 2 (Recovery 1)**: Bấm "Thử lại", proxy chuyển tiếp backend :8000, nhận dữ liệu trạm S03 (`AQI 64`, `PM2.5 18.48 µg/m³`, `simulator`).
- **Ordinal 3 (Timeout)**: Proxy giữ kết nối 15s vượt quá client deadline 10s, abort controller kích hoạt, UI hiển thị lỗi mạng.
- **Ordinal 4 (Recovery 2)**: Bấm "Thử lại", proxy chuyển tiếp backend :8000, nhận dự báo PM2.5 1 giờ tới (`50.06 µg/m³`, `confidence 78%`, `simulator`).
- **Ordinal 5 & 6 (Network Failure)**: Proxy ngắt socket ngay lập tức. Chromium tự động thử lại 1 lần ở tầng TCP (2 transport attempts, `transportAttempts=2`) trước khi ném `TypeError: Failed to fetch`. UI hiển thị thông báo lỗi mạng. Không gọi 2 transport attempts này là 1 outbound request.
- **Ordinal 7 (Recovery 3)**: Bấm "Thử lại", proxy chuyển tiếp backend :8000, nhận kết quả so sánh hai trạm S01 và S05 (`S01 = 43.21 µg/m³`, `S05 = 41.4 µg/m³`).

## 5. Kết quả 7 Cổng Kiểm định Chất lượng
1. `npm run test:api-base-url`: **PASS** (ExitCode: 0)
2. `npm run test:ai-resilience`: **19/19 PASS** (ExitCode: 0)
3. `npm run test:ai-browser-e2e`: **6/6 BROWSER E2E TESTS PASSED** (ExitCode: 0); evidence có **7 proxy records**, `transportAttempts=2`, redaction sạch cookie/CSRF.
4. `npm run build`: **PASS** (2,317 modules transformed, 16.47s, ExitCode: 0)
5. `docker run --rm --entrypoint python p-074-agent@sha256:5bed6699c46d4a22fbbc94e313fa820bb7550a9f6282abfa35ffbd760fff6673 -m pytest tests/test_backend tests/test_agents -q`: **385 passed in 29.77s** (ExitCode: 0). Python 3.11.16; no mounts; 132/132 source/test hashes match, manifest SHA-256 `53ba719f0c4db070ab9cdfec7475080876588343720b6cd8884724bcf4e322c8`.
6. `docker run --rm --entrypoint python p-074-agent@sha256:5bed6699c46d4a22fbbc94e313fa820bb7550a9f6282abfa35ffbd760fff6673 eval/run_evaluation.py`: **62/62 passed (100.0%)**, `release_gate_passed=true`, p95 86.895 ms (ExitCode: 0).
7. `git diff --check`: **Clean** (ExitCode: 0)
- Quản lý cổng mạng: Cả hai cổng `5188` và `19338` đã được giải phóng hoàn toàn sau teardown.

## 6. Kết luận
- **AI-24**: Chuyển trạng thái từ **BLOCKED** sang **PASS**.
- Toàn bộ ma trận kiểm thử 32 cases: **32 PASS, 0 FAIL, 0 BLOCKED**.
- Phiên 3F chính thức hoàn thành.
