# Daily — 2026-08-14

## Yesterday

- Đồng nhất giao diện chi tiết trạm và AI Agent theo AQI-first.
- Hiển thị đầy đủ AQI, PM2.5, CO₂, tiếng ồn và nhiệt độ tại trạm.
- Cập nhật Agent để câu trả lời thông tin trạm trả về AQI trước, sau đó là bốn chỉ số môi trường.
- Bổ sung đánh giá mức độ ảnh hưởng môi trường dựa trên AQI và dữ liệu cùng request.
- Kiểm tra Agent với dữ liệu backend thực tế; test Agent đạt 37 cases.

## Today

- Mở rộng Rule Engine từ cảnh báo PM2.5 sang AQI, PM2.5, CO₂, tiếng ồn và nhiệt độ.
- Mỗi cảnh báo có loại chỉ số, mức độ, giá trị thực đo, ngưỡng, đơn vị và khuyến nghị vận hành.
- Cập nhật màn hình Cảnh báo để hiển thị nội dung và khuyến nghị từ backend.
- Cập nhật Agent đọc cảnh báo active và khuyến nghị do Rule Engine trả về.
- Thêm cấu hình ngưỡng qua environment variables và ADR `0009-multi-metric-environmental-alerts.md`.
- Kiểm tra TypeScript, compile Python và 72 test backend/agent đều pass.

## Blockers

- Docker build/recreate các service đang chạy lâu và đã bị gián đoạn trước khi hoàn tất lần cuối.
- Git chưa commit/push được vì môi trường không cho tạo `.git/index.lock`; nhánh hiện tại là `Canh`.
- Các ngưỡng AQI/CO₂/tiếng ồn/nhiệt độ hiện là ngưỡng MVP provisional, cần mentor/operations xác nhận trước khi dùng ngoài demo.

## Next

- Hoàn tất recreate Backend, Agent và Frontend bằng Docker.
- Smoke test `GET /api/v1/alerts` và kiểm tra cảnh báo có khuyến nghị trên giao diện.
- Commit với message: `feat: unify AQI metrics and add environmental threshold alerts`.
- Push commit lên nhánh `Canh`.
